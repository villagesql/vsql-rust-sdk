# villagesql-sys

Raw FFI bindings for the VillageSQL extension ABI.

This crate is an implementation detail of the [`villagesql`](https://crates.io/crates/villagesql) SDK. Extension authors should depend on `villagesql` directly rather than using this crate.
