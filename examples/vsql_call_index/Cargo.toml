[package]
name = "vsql_call_index"
version = "0.1.0"
edition = "2021"
description = "Example VillageSQL extension: a statful VDF that counts calls via prerun/postrun"
license = "GPL-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
villagesql = { path = "../../villagesql" }
